apiVersion: v1
kind: Service
metadata:
  name: final-app-service
spec:
  selector:
    app: final-app
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
  type: NodePort
